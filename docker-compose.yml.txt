version: '3.2'

networks:
  default:
    external:
      name: mongo-network
services:
  mongodb:
    image: mongo
	container_name: mongodb
    ports:
    - 27017:27017
  springdemonew:
    image: springdemonew
    container_name: springdemonew
    ports:
      - 8080:8080
    links:
      - mongodb